<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="BoardMaster">
	<select id="list" resultType="BoardMaster">
		SELECT * FROM board_master
	</select>
	
	<insert id="add">
		INSERT INTO board_master
		VALUES (null, #{subject}, #{reply}, #{attach}, #{secret}, #{permission})
		
		
	</insert>
	
	<select id="item" resultType="Board">
		SELECT * FROM board_master
		WHERE boardId=#{boardId}
	</select>
	
	<update id="update">
		UPDATE board_master
		SET subject=#{subject}, reply=#{reply}, attach=#{attach}, secret=#{secret}, permission=#{permission}
		WHERE  boardId=#{boardId}
	</update>
	
	<delete id="delete">
		DELETE FROM board_master
		WHERE  boardId=#{boardId}
	</delete>
	
	<update id="create_board">
	CREATE TABLE 'board_${boardId}' (
		'article_id' INT NOT NULL, 
		'subject' VARCHAR(128) NOT NULL, 
		'contents' LONGTEXT NULL, 
		'reg_date' DATE NOT NULL, 
		'view_count' INT NULL DEFAULT 0, 
		'member_id' NULL VARCHAR(32),
		
		CONSTRAINT 'PK_board_${boardId}'	PRIMARY KEY ('article_id'),
		CONSTRAINT 'FK_member_TO_board_${boardId}'	FOREIGN KEY ('member_id')
		REFERENCES 'member' ('id')
	)
	</update>
	
	<update id="delete_board">
		DROP TABLE board_${boardId}
	</update>
	
	<update id="create_board_seq">
		CREATE SEQUENCE BOARD_${boardId}_SEQ
	</update>
	
	<update id="delete_board_seq">
		DROP SEQUENCE BOARD_${boardId}_SEQ
	</update>
	<select id="boardid" resultType="Integer">
		select max(board_id)
		from board_master
	</select>
</mapper>